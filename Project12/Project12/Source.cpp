#include <iostream>
using namespace std;
int main() {
	int cookie;
	char input;
	switch (cookie) {
		while (1) {
	case 1:
		cout << "What kind of cookie would you like?" << endl;
		cout << "Chocolate(c), Raisin(r), Sugar cookie(s), Oatmeal(o), Fish cookie(f), Frosted(ff)" << endl;
		}
	}
}